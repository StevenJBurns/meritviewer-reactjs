import React from 'react';
import { render } from 'react-testing-library';
import { AppMain } from '.';

describe('<AppMain />', () => {
  // afterEach(cleanup);

  it('renders without errors', () => {
    // const {container, debug } = render(<AppMain />);
    // debug();
  });
});
